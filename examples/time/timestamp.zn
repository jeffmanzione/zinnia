import async
import io
import time

now = time.current_timestamp()
io.println(now, now.to_micros(), time.timestamp_from_micros(now.to_micros()))

await async.sleep(1)
now = time.current_timestamp()
io.println(now, now.to_micros(), time.timestamp_from_micros(now.to_micros()))
