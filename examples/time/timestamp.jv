import io
import process
import time

now = time.now()
io.println(now, now.to_micros(), time.timestamp_micros(now.to_micros()))
await process.sleep(1)
now = time.now()
io.println(now, now.to_micros(), time.timestamp_micros(now.to_micros()))
