import io

class Message {
  static HELLO = Message('Hello, world!')
  static BYE = Message('Goodbye!')

  new(field message) {}
  method to_s() message
}

io.println(Message.HELLO)
io.println(Message.BYE)
